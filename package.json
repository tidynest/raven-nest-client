{
  "name": "raven-nest-client",
  "version": "0.2.9",
  "description": "TypeScript MCP client for raven-nest-mcp",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "bun run index.ts",
    "test": "bun test",
    "compile": "bun build --compile ./index.ts --outfile raven-nest-client",
    "badges": "bun run scripts/gen-badges.ts",
    "version": "bun run badges && git add badges"
  },
  "license": "MIT",
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
